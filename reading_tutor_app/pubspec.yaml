name: reading_tutor_app
description: "This project is build for the student tutor project"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Database - SQLite
  sqflite: ^2.3.0
  path: ^1.8.3

  # API calls
  http: ^1.2.0

  # State management
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true